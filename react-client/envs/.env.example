# Vite の envDir=react-client/envs
# nginx 経由（http://localhost）で同一オリジンにする。直接 backend を叩く場合は http://localhost:8000
#
# 実ファイル (.env.dev / .env.prod) は dotenvx で暗号化する。
# 値の変更: dotenvx set VITE_API_BASE_URL "http://localhost" -f envs/.env.dev --no-native
VITE_API_BASE_URL=http://localhost
