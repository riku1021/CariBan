import { css } from "@/styled-system/css";

export const page = css({
  display: "flex",
  flexDirection: "column",
  gap: "md",
});

export const heading = css({
  fontSize: "2xl",
  fontWeight: "bold",
  color: "text.main",
  margin: "0",
});
