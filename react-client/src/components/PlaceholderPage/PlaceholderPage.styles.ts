import { css } from "@/styled-system/css";

export const page = css({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: "100%",
  minHeight: "0",
  padding: "sm",
});

export const message = css({
  margin: "0",
  fontSize: "sm",
  color: "text.sub",
});
